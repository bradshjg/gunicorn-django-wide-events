[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "gunicorn-django-canonical-logs"
dynamic = ["version"]
description = "Canonical log lines for Django applications using Gunicorn sync workers"
readme = "README.md"
requires-python = ">=3.9"
license = "MIT"
keywords = []
authors = [
  { name = "James Bradshaw", email = "james.g.bradshaw@gmail.com" },
]
classifiers = [
  "Development Status :: 4 - Beta",
  "Programming Language :: Python",
  "Programming Language :: Python :: 3.9",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Programming Language :: Python :: Implementation :: CPython",
]
dependencies = [
  "django",
  "gunicorn",
  "posix-ipc",
  "psutil",
]

[project.urls]
Documentation = "https://github.com/bradshjg/gunicorn-django-canonical-logs#readme"
Issues = "https://github.com/bradshjg/gunicorn-django-canonical-logs/issues"
Source = "https://github.com/bradshjg/gunicorn-django-canonical-logs"

[project.optional-dependencies]
test = [
  "pytest~=8.1",
  "pytest-django~=4.11",
  "pytest-mock~=3.12",
  "pytest-randomly~=3.15",
  "pytest-rerunfailures~=14.0",
  "pytest-xdist[psutil]~=3.5",
  "requests",
  "whitenoise",
]

[tool.hatch.version]
path = "src/gunicorn_django_canonical_logs/__about__.py"

[tool.hatch.envs.types]
features = [
  "test",
]
extra-dependencies = [
  "mypy>=1.0.0",
]

[tool.hatch.envs.types.scripts]
check = "mypy --install-types --non-interactive --ignore-missing-imports {args:src/gunicorn_django_canonical_logs tests}"

[[tool.hatch.envs.hatch-test.matrix]]
python = ["3.10", "3.13"]
django = ["4.2", "latest"]

[tool.hatch.envs.hatch-test]
extra-args = ["-vv"]
features = [
  "test",
]

[tool.hatch.envs.hatch-test.overrides]
matrix.django.dependencies = [
  { value = "django~=4.2", if = ["4.2"] }
]

[tool.hatch.envs.dev]
path = ".venv/dev"
features = [
  "test",
]

[tool.hatch.envs.server]
features = [
  "test",
]

[tool.hatch.envs.server.scripts]
start = "cd tests/server && gunicorn --bind 127.0.0.1:8080 -c gunicorn_config.py app"

[tool.pytest.ini_options]
django_find_project = false
DJANGO_SETTINGS_MODULE = "settings"
pythonpath = "tests/server"
