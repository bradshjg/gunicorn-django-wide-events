# Development

This project prefers a VS Code devcontainer environment, and the below expects that.

## Running tests

`hatch test`

## Linting/Formatting

`hatch fmt`

## Running local gunicorn server

`hatch run server:start`
